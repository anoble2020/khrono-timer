name: Dev Deploy

on:
  push:
    branches: [ dev ]

jobs:
  dev-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci --legacy-peer-deps
      
    - name: Run linter
      run: npm run lint
      
    - name: Build project
      run: npm run build
      
    - name: Deploy to dev environment
      run: |
        echo "🚀 Deploying dev build..."
        echo "Build completed successfully for dev branch"
        # Add your dev deployment logic here
        # For example: deploy to a staging server, update dev app, etc.
